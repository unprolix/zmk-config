# ZMK's underglow owns the LED strip and drives it only as one animated whole,
# so it has to be out of the way before individual pixels can be addressed.
CONFIG_ZMK_RGB_UNDERGLOW=n

# External power feeds the strip; without it the chain stays dark whatever is
# written to it.
CONFIG_ZMK_EXT_POWER=y
