if SHIELD_TOUCAN_DISPLAY

config ZMK_DISPLAY
    default y

config LS0XX_VCOM_THREAD_PRIO
    default 11

config LV_Z_VDB_SIZE
    default 100

config LV_DPI_DEF
    default 161

config LV_Z_BITS_PER_PIXEL
    default 1

choice LV_COLOR_DEPTH
    default LV_COLOR_DEPTH_1
endchoice

choice ZMK_DISPLAY_WORK_QUEUE
    default ZMK_DISPLAY_WORK_QUEUE_DEDICATED
endchoice

# Built-in status screen: no custom widget C, so this builds from the
# config directory (beekeeb's nice_view_gem widgets need a zephyr module).
choice ZMK_DISPLAY_STATUS_SCREEN
    default ZMK_DISPLAY_STATUS_SCREEN_BUILT_IN
endchoice

# 144x168 is ~2.2x the pixel count of a nice!view; give LVGL more headroom.
config LV_Z_MEM_POOL_SIZE
    default 8192

endif
