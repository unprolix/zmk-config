# DIAGNOSTIC ONLY. A standalone (non-split) image, so it enumerates on USB with
# its own raw-HID trigger when built with the dfu shield:
#     scripts/dfu-trigger.py central --name pinscan
CONFIG_ZMK_KEYBOARD_NAME="OP36 pinscan"
CONFIG_ZMK_SLEEP=n
